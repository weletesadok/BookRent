import { Box } from "@mui/material";

export default () => {
  return (
    <Box sx={{ padding: "1rem", height: "65%" }}>
      Live Book status. on progress available soon. check the users and books.
    </Box>
  );
};
